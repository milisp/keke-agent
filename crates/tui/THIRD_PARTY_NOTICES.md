# Third-party notices

## grok-build

`src/ported/grok_build/at_context.rs` is ported from
`crates/codegen/xai-grok-pager/src/views/file_search/context.rs` in
grok-build, licensed Apache-2.0.

`src/ported/grok_build/format_duration.rs` is ported from
`crates/codegen/xai-grok-pager-render/src/util.rs` (`format_duration`) in
grok-build, licensed Apache-2.0, with the day tier added.
